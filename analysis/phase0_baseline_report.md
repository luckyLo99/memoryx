# MemoryX ?????????

---

## ?????????????

### ????
| ?? | ? |
|------|----|
| ??? | `D:\lucky\Documents\MemoryX 2` |
| Git ?? | `memoryx-2-kernel` |
| ?? | 2.1.1 |
| Python | >=3.11 |
| ?? | setuptools (pyproject.toml) |
| ??? | 53 ? |
| Python ?? | 303 ? |

### Git ??
- ??: `memoryx-2-kernel`
- ?????: **??**?Phase A-F ?????
- ???: tests ??????????? `tests/unit/`?
- ?????: ??????Phase A-F ???????
- ??: **????? commit ??????**

### ???????
| ?? | ?? |
|------|------|
| pyproject.toml | ? ?? |
| README.md | ? ?? |
| pytest.ini | ? ?? |
| AGENTS.md | ? ??? |
| VERSION | ? ?? (2.1.1) |
| memoryx/AGENTS.md | ? ?? (???) |

---

## ???????????

### ???????

#### Hermes ? MemoryX ?????

```
Hermes Agent
  |
  v
integrations/hermes/plugins/memoryx_runtime/__init__.py  (P13 gate + hooks)
  |
  v
integrations/hermes/memory_provider/memoryx/__init__.py  (MemoryXProvider)
  |  ??? ?? MemoryProvider ??
  |  ??? ?? memory() tool handler
  |  ??? ???? (_run_async)
  |
  v
memoryx/hermes/bridge.py  (HermesMemoryBridge)
  |  ??? on_user_message() ? LLM ??? + ?? + ?????
  |  ??? on_tool_call() ? ????
  |  ??? on_session_end() ? ???? + ????
  |
  v
memoryx/hermes/provider.py  (MemoryXHermesProvider)
  |  ??? add/read/list/replace/remove ??
  |  ??? ? MemoryCandidateService ??
  |
  v
memoryx/services/memory_candidate_service.py  (??????)
  |  ??? candidate ? verify ? commit ? reject/supersede
  |
  v
memoryx/storage/repository.py  (MemoryRepository - SQLite + FTS5)
```

#### ?????
```
MemoryXHermesProvider.action("add", ...)
  ? MemoryCandidateService.submit_candidate() + promote_to_committed()
  ? MemoryRepository.store_memories()
  ? SQLite (memories ?) + FTS5 (fts_memories)
```

#### ??????
```
query ? HybridRetrievalEngine.search()
  ??? FTS5 keyword search
  ??? Vector search (?? embedding)
  ??? Temporal scoring
  ??? RRF fusion
  ??? Score breakdown + ??
```

#### MCP ????
```
MCP Client ? memoryx/mcp/server.py (MCPServer)
  ??? memory_query ? retrieval/engine.py
  ??? memory_store ? MemoryCandidateService
  ??? memory_signal ? core/hermes_adapter.py (LEGACY!)
```

---

### Legacy Core ??????

#### ??? `memoryx.core` ?????

| ?? | ???? | ???? |
|------|----------|----------|
| `memoryx/context_budget/assembler.py` | HybridRetriever, SearchOptions | ?? **??** |
| `memoryx/mcp/adapter.py` | HermesAdapter, MemoryKernel, HybridRetriever, SearchOptions | ?? **??** |
| `memoryx/core/hermes_adapter.py` | HybridRetriever (???) | ?? ?? |
| `tests/unit/context/test_context_*.py` | HermesAdapter, MemoryKernel | ?? ?? |

#### ?? core/ ?????????

| ? | ?? | ?? |
|----|------|------|
| `MemoryKernel` | core/kernel.py | MCP???????? |
| `Retriever` | core/retriever.py | core ???? |
| `HybridRetriever` | core/hybrid_retriever.py | assembler?adapter ?? |
| `HermesAdapter` | core/hermes_adapter.py | MCP adapter????? |
| `Schema` (? claims/evidence) | core/schema.py | ? core ???? |

#### ??????

| ?? | ???? | ???? |
|------|----------|----------|
| ?? (scoring) | `retrieval/scorer.py` | `core/scoring.py` (??) |
| ?? (fusion) | `retrieval/fusion.py` | `core/fusion.py` (??) |
| ???? | `cognitive/conflict.py` | `core/conflict.py` (??) |
| ???? | `embeddings/vector_store.py` | `core/vector.py` (??) |
| ???? | `retrieval/engine.py` | `core/hybrid_retriever.py` (????) |

---

### ????????

```bash
# ???????????
python -m pytest tests/unit/misc/test_scoring.py tests/unit/misc/test_fusion.py -v

# ????
python -m pytest tests/unit/storage/ -v

# ????
python -m pytest tests/unit/retrieval/ -v

# Hermes/MCP ????
python -m pytest tests/unit/hermes/ tests/unit/mcp/ -v

# ??????????????
python -m pytest tests/unit/misc/ -v --tb=short -p no:cacheprovider

# ????????
MEMORYX_DATA_DIR=.codex-runtime/memoryx-test \
MEMORYX_TEST_MODE=1 \
PYTHONUTF8=1 \
python -m pytest tests/ -v
```

---

## ????????????

### Phase 0: ?????????

**??**: ???????????????

**????**:
- `pytest.ini` (??????????)
- `tests/conftest.py` (??????????)
- ?? `.codex-runtime/memoryx-test/` (??????)

**??**: ? ? ???????????????

**????**:
```bash
python -m pytest tests/unit/misc/test_scoring.py -v
python -m pytest tests/unit/misc/test_fusion.py -v
```

---

### Phase 1: ?????? Legacy Import

**??**: ?? 3 ???? `memoryx.core` ?????

**????**:
1. `memoryx/context_budget/assembler.py` ? ? core ?? HybridRetriever
2. `memoryx/mcp/adapter.py` ? ? core ?? HermesAdapter, MemoryKernel
3. `memoryx/core/hermes_adapter.py` ? ?????

**??**: ?? ? ? MCP adapter ? context assembler ?????

**??**:
- ? `core/hybrid_retriever.py` ? `HybridRetriever.search()` ????? `retrieval/engine.py`
- ? `core/kernel.py` ? `MemoryKernel` ????? `storage/repository.py`
- ? `core/` ?????

**????**:
```bash
python -m pytest tests/unit/misc/test_hybrid_retriever.py
python -m pytest tests/unit/misc/test_phase2_retriever.py
python -m pytest tests/unit/context/
```

---

### Phase 2: ???????

**??**: ????????? `MemoryCandidateService`

**????**:
- `memoryx/hermes/provider.py` (???)
- `memoryx/mcp/server.py` (MCP ???)
- `memoryx/api/plugin.py` (API ???)
- `memoryx/services/memory_candidate_service.py` (??????)

**??**: ?? ? ? ???????

---

### Phase 3: ????????? (??)

**??**: ?? `retrieval/engine.py` ???????

---

### Phase 4-7: (????)

---

## ????????? Patch

### ??: ? git commit ????

```bash
git add -A
git commit -m "chore: baseline Phase A-F restructuring complete"
```

### ???? Phase 0

???:
1. ?? `pytest.ini` ????
2. ?? `tests/conftest.py` ???????
3. ??????

---
