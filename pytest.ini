[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
norecursedirs = memoryx-pure-release reports artifacts .git .venv dist build scripts


markers =
    benchmark: competitive benchmark tests (requires API keys for external systems)
    slow: slow tests that may need network or heavy compute
